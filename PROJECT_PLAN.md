# TermResume 项目规划

> 文档状态：第一版产品与技术方向草案  
> 更新日期：2026-09-04  
> 当前阶段：终端简历 MVP 规划

## 1. 项目定位

TermResume 是一个以终端交互为核心的个人简历与个人主页系统。

访客不只是从上到下阅读一张静态简历，而是像进入一台个人计算机一样，通过
`help`、`ls`、`cd`、`cat`、`tree`、`goto` 等命令主动探索个人资料、技术能力、
项目经历、获奖证书和博客内容。

项目同时提供管理模式，让维护者可以持续更新这些内容。后续计划加入个人 Agent，
使访客能够通过自然语言对话了解站点作者，并获得更接近本人表达方式的介绍与回答。

项目不是一次性的视觉 Demo，而是一个可以长期维护、持续扩展并最终公开部署的个人数字主页。

## 2. 核心目标

### 2.1 产品目标

- 建立具有辨识度的终端风格个人简历。
- 让访客通过命令探索内容，同时不要求访客熟悉真实 Linux。
- 使用结构化数据管理简历内容，避免内容散落在页面和脚本中。
- 同时支持 guest 浏览和 admin 内容维护。
- 保留图形化页面，兼顾快速浏览、移动端使用和管理效率。
- 为未来接入后端、数据库和个人 Agent 预留稳定边界。

### 2.2 当前阶段目标

当前阶段优先完成一个可靠、可展示、可维护的纯前端版本：

- 终端浏览体验完整且稳定。
- 简历核心内容覆盖完整。
- guest 与 admin 权限表现清晰。
- admin 可以完成主要内容的增删改查。
- 数据可以导入和导出，降低 LocalStorage 丢失风险。
- 前端形成统一、克制、专业的终端视觉风格。

### 2.3 暂不承诺的能力

- 当前纯前端登录不作为安全认证方案。
- 当前 LocalStorage 数据不会自动同步给其他访客或设备。
- 第一阶段不训练、不微调也不蒸馏大模型。
- 第一阶段不购买服务器，不提前绑定具体云服务。

## 3. 产品原则

### 3.1 终端优先，图形页面辅助

终端是项目的核心识别点和默认入口。图形页面用于：

- 帮助不熟悉命令行的访客快速浏览。
- 在手机等不适合大量键盘输入的设备上提供完整体验。
- 为 admin 提供效率更高的表单和内容管理界面。

终端与图形页面必须读取同一份数据，不能形成两套互相独立的内容。

### 3.2 体验像终端，业务不绑死终端

命令是用户界面，不是底层数据结构。终端命令、图形页面和 admin 表单都通过统一的数据服务读取和修改内容。

```text
终端命令 ----\
图形页面 ----- > 数据服务 -> LocalStorage / 本地数据（当前）
Admin 页面 ---/             -> 远程 API / 数据库（未来）
```

这样可以保证未来接入后端时，终端命令和页面不需要整体重写。

### 3.3 易探索，不故意制造门槛

终端风格不等于让访客猜命令。系统应持续提供轻量引导：

- 首屏展示少量推荐命令。
- `help` 提供完整命令分类。
- Tab 支持命令和路径补全。
- 输入错误时提供相近命令建议。
- 图形页面始终可以通过 `goto` 或导航访问。
- 不阻止访客跳过启动动画。

### 3.4 内容可信，Agent 不编造

未来 Agent 的回答必须优先来源于结构化简历、博客、项目复盘和经过维护者确认的材料。
不知道的信息应明确说明，不应自动虚构经历、技术能力或个人观点。

## 4. 用户与权限

### 4.1 Guest 游客

Guest 无需登录，主要任务是了解站点作者。

允许：

- 浏览个人介绍、联系方式和技能。
- 浏览教育、实习、工作和其他经历。
- 浏览项目、获奖、证书和博客。
- 使用终端只读命令探索虚拟文件系统。
- 使用 `goto` 进入图形化页面。
- 后期使用 `intro`、`ask` 或 `chat` 与个人 Agent 交流。

禁止：

- 新增、修改或删除公开内容。
- 查看仅供 Agent 使用的私有材料。
- 访问 admin 管理操作和敏感配置。

### 4.2 Admin 管理者

Admin 是站点作者本人，主要任务是维护内容。

允许：

- 管理个人资料、经历、技能和联系方式。
- 管理项目、获奖、证书与博客。
- 管理头像、项目图片和证明材料。
- 导入、导出和恢复数据。
- 后期管理 Agent 知识材料及其公开范围。

当前阶段的 admin 是本地维护工具，不具备真正的线上安全性。正式上线远程管理前，必须改为服务端认证和服务端权限校验。

## 5. 信息架构

建议将简历内容映射为稳定的虚拟目录，让命令浏览和结构化数据保持一致。

```text
~
├── README.md                 访问指南
├── about.md                  个人介绍
├── contact.md                联系方式
├── skills/                   技能分类
│   ├── frontend.md
│   ├── backend.md
│   └── tools.md
├── experience/               教育、实习、工作和实践经历
│   ├── education.md
│   └── practice.md
├── projects/                 项目列表和详情
│   └── <project-slug>/
│       └── README.md
├── awards/                   获奖记录
├── certificates/             证书记录
├── blog/                     博客文章
└── agent/                    Agent 入口说明，不暴露私有知识原文
```

虚拟文件内容应由结构化数据动态生成，而不是再维护一份独立静态文本。

## 6. 核心功能范围

### 6.1 终端系统

保留并完善以下基础能力：

- 启动动画与可跳过机制。
- 当前用户、主机名和路径提示符。
- 命令输入、执行结果和错误输出。
- 上下键历史、Tab 补全、命令别名。
- `&&` 串联执行。
- 环境变量展开。
- 文件路径解析和简化权限表现。
- 桌面与移动端可用的输入体验。

核心浏览命令：

| 命令 | 用途 |
| --- | --- |
| `help [command]` | 查看命令列表或命令帮助 |
| `pwd` | 查看当前位置 |
| `ls [-la] [path]` | 浏览目录 |
| `cd [path]` | 切换目录 |
| `tree [-a] [path]` | 查看目录结构 |
| `cat <file>` | 查看完整内容 |
| `head <file>` | 查看文件开头 |
| `tail <file>` | 查看文件结尾 |
| `whoami` | 查看当前身份 |
| `neofetch` | 展示系统和个人摘要 |
| `history` | 查看命令历史 |
| `clear` | 清空当前终端输出 |
| `goto <page>` | 打开图形化页面 |

简历快捷命令：

| 命令 | 用途 |
| --- | --- |
| `intro` | 输出一段结构化自我介绍，后期由 Agent 增强 |
| `resume` | 查看简历摘要 |
| `projects` | 查看项目列表 |
| `skills` | 查看技能摘要 |
| `experience` | 查看经历时间线 |
| `awards` | Guest 查看、Admin 管理获奖 |
| `certs` | Guest 查看、Admin 管理证书 |
| `blog` | Guest 阅读、Admin 管理博客 |
| `contact` | 查看公开联系方式 |

第一阶段可以让快捷命令调用已有数据查询；不要求每个功能都伪装成真实 Linux 命令。

### 6.2 图形化浏览页面

保留以下页面：

- 个人概览。
- 经历时间线。
- 技能分类。
- 项目列表与项目详情。
- 获奖记录。
- 证书画廊。
- 博客时间线与文章详情。
- 命令说明书。
- 后期增加 Agent 对话页。

图形页和终端之间应能随时切换，并保持必要的上下文。例如从项目图形页返回终端时，可以保留此前的终端输出与路径。

### 6.3 Admin 内容管理

Admin 应支持以下标准操作：

| 数据 | 查看 | 新增 | 编辑 | 删除 | 排序 |
| --- | --- | --- | --- | --- | --- |
| 个人资料 | 是 | 不适用 | 是 | 不适用 | 不适用 |
| 经历 | 是 | 是 | 是 | 是 | 是 |
| 技能 | 是 | 是 | 是 | 是 | 是 |
| 项目 | 是 | 是 | 是 | 是 | 是 |
| 获奖 | 是 | 是 | 是 | 是 | 是 |
| 证书 | 是 | 是 | 是 | 是 | 是 |
| 博客 | 是 | 是 | 是 | 是 | 按日期 |
| Agent 材料 | 后期 | 后期 | 后期 | 后期 | 后期 |

管理方式：

- 终端命令适合查看、快速添加和删除。
- 图形化表单负责完整编辑、长文本、图片和排序。
- 删除重要内容前应有确认。
- 保存结果必须明确显示成功或失败。
- 表单应提供必填校验和错误提示。

### 6.4 数据保护

LocalStorage 阶段至少提供：

- 导出完整 JSON 数据。
- 从 JSON 导入并校验数据。
- 恢复默认数据。
- 导入前创建当前数据备份。
- LocalStorage 写入失败时显示真实错误。
- 数据增加 `schemaVersion`，为以后迁移字段做准备。

## 7. 数据模型

所有实体使用稳定 ID，不依赖数组下标。日期统一使用 `YYYY-MM-DD`，排序字段使用数字。

### 7.1 Profile

```js
{
  name,
  role,
  bio,
  location,
  email,
  github,
  website,
  avatar,
  availableForWork
}
```

### 7.2 Experience

```js
{
  id,
  type,          // education | internship | work | activity
  organization,
  role,
  startDate,
  endDate,
  location,
  summary,
  highlights,
  sortOrder,
  visible
}
```

### 7.3 Skill

```js
{
  id,
  name,
  category,
  level,
  keywords,
  evidence,      // 可关联项目 ID
  sortOrder,
  visible
}
```

### 7.4 Project

```js
{
  id,
  slug,
  name,
  summary,
  description,
  techStack,
  highlights,
  repositoryUrl,
  demoUrl,
  images,
  startDate,
  endDate,
  featured,
  sortOrder,
  visible
}
```

### 7.5 Award 与 Certificate

```js
{
  id,
  title,
  issuer,
  level,
  date,
  description,
  image,
  relatedProjectIds,
  sortOrder,
  visible
}
```

### 7.6 Post

```js
{
  id,
  slug,
  title,
  summary,
  date,
  updatedAt,
  tags,
  content,
  published,
  visible
}
```

### 7.7 AgentSource（后期）

```js
{
  id,
  title,
  type,          // faq | story | opinion | project-note | biography
  content,
  visibility,    // public | agent-only | private
  tags,
  updatedAt,
  enabled
}
```

`private` 数据禁止发送给访客侧 Agent。最终权限必须由服务端执行，不能只依赖前端隐藏。

## 8. 技术架构

### 8.1 当前纯前端阶段

```text
UI 层
├── Terminal View
├── Graphic Pages
└── Admin Forms
        |
Command / Controller 层
        |
Domain Service 层
├── ProfileService
├── ProjectService
├── ResumeService
├── PostService
└── StorageService
        |
LocalStorage Adapter + Default Data
```

建议逐步把当前全局函数拆成职责明确的模块，但不为拆分而拆分。第一步先形成统一数据服务，再决定是否迁移到 ES Modules。

### 8.2 未来线上阶段

```text
静态前端（GitHub Pages / Vercel）
        |
        | HTTPS
        v
API / Serverless Functions
├── Authentication
├── Resume CRUD
├── Media Upload
└── Agent Gateway
        |
        +--> Database
        +--> Object Storage
        +--> Vector Index
        +--> Model API
```

无论是否购买服务器，前端都只调用约定好的服务接口。这样可以从 LocalStorage 平滑替换到 Serverless 或自建后端。

## 9. 前端视觉与交互规划

### 9.1 设计方向

关键词：专业、克制、可信、可探索、现代终端。

终端感主要来自排版、层级、反馈和交互，不依赖大量荧光、扫描线或装饰动画。页面应首先是一份好读的简历，其次才是终端主题作品。

### 9.2 视觉系统

- 主体使用深色中性背景，绿色作为命令和成功状态，青色作为路径和链接。
- 黄色用于提醒，红色仅用于错误或危险操作。
- 避免整页被单一绿色覆盖，正文保持高对比中性色。
- 使用等宽字体展示终端；长文章和详细说明可使用更易读的正文栈。
- 统一边框、阴影、间距和圆角，卡片圆角控制在较小范围。
- 白天模式不是简单反色，应保持同样的语义色和信息层级。
- 动画以 120 至 240 毫秒的状态反馈为主，启动动画允许跳过并记住偏好。

### 9.3 终端界面优化

- 顶栏显示当前会话、窗口状态和主题控制。
- 提示符清楚区分 guest `$` 与 admin `#`。
- 输入框始终固定，不因历史输出增长而移动。
- 命令、参数、路径、错误和提示采用稳定的语义颜色。
- 长输出提供合理换行，代码块支持横向滚动。
- 命令执行期间给出轻量状态，不伪造无意义的长时间加载。
- 首屏推荐 3 至 4 个高价值命令，避免一次展示全部功能。
- 未知命令提供纠错建议，并允许点击建议自动填入。
- 移动端提供常用命令快捷栏，但不遮挡输入区和输出区。

### 9.4 图形页面优化

- 页面导航紧凑、稳定，当前页面状态明确。
- 简历内容优先采用时间线、列表和清晰分组，不堆叠装饰性卡片。
- 项目卡片展示真实项目名称、职责、技术栈、结果和入口。
- 获奖与证书页面接入真实图片，并提供加载失败状态。
- 博客阅读页提高正文宽度、字号和代码块可读性。
- Admin 表单与公开内容在视觉上明确区分。
- 删除、保存、校验失败和空状态都有完整反馈。

### 9.5 可访问性与响应式

- 所有核心能力可以只用键盘完成。
- 焦点样式清晰，不移除浏览器焦点提示。
- 颜色不是状态的唯一表达方式。
- 尊重 `prefers-reduced-motion`。
- 桌面端保证终端是首屏主角；移动端保证输入框、快捷操作和正文互不遮挡。
- 主要页面至少验证 360px、768px、1280px 和宽屏布局。

## 10. 个人 Agent 规划

### 10.1 Agent 的角色

Agent 是作者的数字介绍者，不是无边界通用聊天机器人。它主要回答：

- 你是谁、目前在做什么。
- 做过哪些项目，负责过什么，遇到过什么问题。
- 掌握哪些技术，有哪些实际证据。
- 为什么做这个终端简历项目。
- 对学习、工程实践和职业方向有哪些公开观点。

### 10.2 推荐交互

```bash
intro
ask 你为什么做这个项目？
ask 介绍一下你最满意的作品
ask 你擅长哪些前端技术？
chat
```

- `intro` 给出简短、稳定的自我介绍。
- `ask <question>` 完成一次问答。
- `chat` 进入连续对话模式，支持 `exit` 返回普通终端。
- 图形页提供等价的对话界面，移动端不强迫输入命令。

### 10.3 实现阶段

第一步：模板化介绍。

- 根据 Profile、Experience、Project 自动生成可靠介绍。
- 不调用模型，先确定内容结构和交互方式。

第二步：提示词加检索增强生成（RAG）。

- 将公开简历、博客、项目复盘和 FAQ 建立检索索引。
- 模型回答时附带数据来源或相关项目入口。
- 无资料支持时明确表示不知道。

第三步：个性表达优化。

- 收集本人撰写的自我介绍、项目复盘和高质量问答。
- 建立语气、措辞、边界和禁答规则。
- 使用固定评测问题验证事实准确性和表达一致性。

第四步：评估微调或蒸馏。

- 只有在积累足够高质量材料，并确认提示词与 RAG 无法满足需求后再考虑。
- 蒸馏目标应是表达一致性、响应成本或私有部署，而不是单纯提高项目复杂度。

### 10.4 Agent 安全边界

- 模型 API Key 不得放在浏览器源码中。
- Agent 请求必须经过服务端或受保护的网关。
- 私人联系方式、未公开经历和敏感材料不得进入公开检索库。
- 对输入长度、频率和输出长度进行限制。
- 保存对话前明确告知用户，并避免默认收集敏感数据。

## 11. 部署策略

### 11.1 当前阶段

使用 GitHub Pages 或其他静态托管发布只读展示版本。Admin 作为本地维护能力保留，并通过导出 JSON 或更新仓库发布新内容。

优点：

- 免费或成本很低。
- 部署简单，适合快速迭代。
- 当前终端和图形页面都可以直接运行。

限制：

- 无法提供安全的线上 Admin。
- 无法保存所有访客共享的数据。
- 不能安全保存模型密钥。

### 11.2 Agent 或远程管理阶段

优先评估静态前端加 Serverless API，不急于购买长期服务器。只有出现以下情况时再考虑自建服务：

- 需要运行常驻 Agent 或自托管模型。
- Serverless 执行时长、流式响应或资源限制不满足需求。
- 需要完整控制数据库、日志和部署环境。
- 实际使用成本已经高于服务器维护成本。

## 12. 分阶段实施计划

### 阶段 A：基础治理与数据统一

目标：让当前原型成为可继续开发的稳定基础。

- [x] 修复博客命令参数解析问题。
- [x] 让保存函数返回成功或失败，界面显示真实结果。
- [x] 为数据加入 `schemaVersion`。
- [x] 统一博客图形页、终端命令和虚拟文件系统的数据源。
- [x] 统一个人资料默认值，减少 `CONFIG` 与 Profile 重复。
- [x] 接入现有奖项图片并纳入数据模型。
- [x] 补充最小数据导出和导入能力。

验收标准：同一内容在终端与图形页面一致；刷新后数据不丢失；保存失败不会显示成功。

### 阶段 B：完整简历内容

目标：从“有趣的终端 Demo”升级为“内容完整的个人简历”。

- [ ] 增加教育、实践、实习或工作经历模型。
- [ ] 增加结构化技能模型。
- [ ] 将项目从静态 VFS 内容迁移为结构化数据。
- [ ] 增加项目详情、项目链接和图片。
- [ ] 增加 `intro`、`skills`、`experience`、`projects`、`contact` 快捷命令。
- [ ] 补充真实个人内容，清理示例数据。

验收标准：访客只通过终端即可了解完整简历；图形页可以在较短时间内快速扫读同一份内容。

### 阶段 C：Admin CRUD 完整化

目标：所有主要简历内容都可以持续维护。

- [ ] 补全项目、技能和经历的增删改查。
- [ ] 支持编辑现有获奖和证书，而不只是新增、删除。
- [ ] 增加排序、显示/隐藏和草稿状态。
- [ ] 增加删除确认、表单校验和保存反馈。
- [ ] 增加完整数据备份、导入和恢复流程。
- [ ] 建立存储适配器，为 API 替换做准备。

验收标准：不修改源码即可维护全部公开简历内容，并能可靠备份和恢复。

### 阶段 D：终端视觉与交互升级

目标：在不牺牲可读性的前提下提升辨识度和完成度。

- [ ] 整理颜色、间距、字号和状态变量。
- [ ] 优化首屏信息层级和推荐命令。
- [ ] 优化命令输出、错误、补全和等待反馈。
- [ ] 优化移动端输入与常用命令快捷操作。
- [ ] 改善项目、履历、证书和博客图形页面。
- [ ] 加入减少动画偏好和键盘焦点支持。
- [ ] 在桌面和移动端进行截图与交互回归。

验收标准：终端是第一视觉焦点；信息不拥挤；关键内容无需猜测；桌面和移动端均无重叠、截断或不可操作区域。

### 阶段 E：部署与远程数据

目标：从单浏览器原型升级为公开可维护站点。

- [ ] 确定静态托管平台。
- [ ] 设计并实现内容 API。
- [ ] 接入服务端身份认证和权限检查。
- [ ] 接入数据库与对象存储。
- [ ] 将 LocalStorage Adapter 替换为 API Adapter。
- [ ] 增加基础日志、备份和错误监控。

验收标准：Admin 的修改可以安全地影响所有访客看到的内容；访客无法通过修改前端状态取得写权限。

### 阶段 F：个人 Agent

目标：让访客通过自然语言深入了解作者。

- [ ] 先实现无模型的 `intro` 模板。
- [ ] 整理公开知识材料和标准问题集。
- [ ] 接入服务端 Agent 网关。
- [ ] 实现 RAG 检索与引用来源。
- [ ] 增加事实准确性、隐私和提示注入测试。
- [ ] 根据真实效果决定是否微调或蒸馏。

验收标准：Agent 对核心履历问题回答准确；不会编造经历；不会泄露私有资料；移动端和终端模式均可顺畅使用。

## 13. 质量要求

### 13.1 功能测试

- 路径解析、权限和 glob 展开。
- 命令分词、参数解析和历史遮罩。
- guest/admin 权限边界。
- 各实体 CRUD 与排序。
- LocalStorage 异常、坏数据与版本迁移。
- Hash 路由和终端/图形页切换。
- Markdown 转义和外部链接安全属性。

### 13.2 浏览器验证

- Chrome、Edge 和 Firefox 的当前稳定版本。
- `file://` 直接打开和 HTTP 静态服务两种方式。
- 360px 手机、768px 平板、1280px 桌面和宽屏。
- 昼夜主题、减少动画、键盘导航。

### 13.3 内容质量

- 页面和终端使用同一份事实数据。
- 公开链接可访问，图片有替代文本和错误状态。
- 示例内容在正式发布前全部替换。
- 项目描述强调本人职责、问题、方案和结果，而不只列技术栈。

## 14. 当前已知问题

- admin 账号密码明文位于前端，仅适合本地演示。
- admin 会话可以通过浏览器存储伪造。
- LocalStorage 数据只属于当前浏览器。
- `src/awards/` 已接入页面，但目录当前仍未被 Git 跟踪，提交时需要一并加入。
- 项目、技能和经历尚未统一成可维护实体。
- 尚无自动化测试和持续集成。

## 15. 已对齐的决策

- 项目定位为可长期维护的终端个人简历，不是一次性展示页面。
- 终端是核心体验，图形页是辅助浏览和 Admin 管理入口。
- Guest 只读，Admin 管理内容。
- 当前先完成纯前端可维护版本，不急于购买服务器。
- 架构需要允许 LocalStorage 在未来替换为远程 API。
- Agent 后期接入，先使用可信数据、提示词与 RAG，再评估蒸馏。
- 前端会适度美化，但以专业、可读和终端交互感为优先。

## 16. 下一步

下一轮从阶段 A 开始，优先完成数据统一和基础可靠性，再开始视觉升级。推荐执行顺序：

1. 定义统一的应用数据结构和存储接口。
2. 修复现有博客参数与保存反馈问题。
3. 让虚拟文件系统从统一数据动态生成简历内容。
4. 增加导入、导出和数据版本。
5. 在可靠的数据基础上补充完整简历实体与前端美化。
